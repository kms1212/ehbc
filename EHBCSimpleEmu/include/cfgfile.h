#ifndef __CFGFILE_H__
#define __CFGFILE_H__

/*
#include <stdint.h>
#include <stdlib.h>

struct cpuconfig {

};

struct memconfig {

};

struct aspconfig {

};

struct config {
    struct cpuconfig* cpu;
    size_t cpucnt;

    struct memconfig* mem;
    size_t memcnt;

    struct aspconfig* asp;
    size_t aspcnt;
};

int cfgfile_read(char* path, struct config* cfg);
*/

#endif  // __CFGFILE_H__
